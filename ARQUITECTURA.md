# 🏗️ Arquitectura del Sistema InfectoPlan

## Diagrama de Flujo General

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUARIO                                  │
│                    (Coordinador Médico)                          │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     │ Interacción
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      INTERFAZ (App.tsx)                          │
│  ┌───────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │  Sidebar  │  │ Controls │  │ Calendar │  │    Modal     │  │
│  │ (Personal)│  │(Nav+Btn) │  │  (Grid)  │  │  (Edición)   │  │
│  └───────────┘  └──────────┘  └──────────┘  └──────────────┘  │
└────────────────────┬────────────────────────────────────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼                     ▼
┌──────────────────┐   ┌──────────────────┐
│    SCHEDULER     │   │  STORAGE SERVICE │
│  (Algoritmo CSP) │   │  (LocalStorage)  │
│                  │   │                  │
│ • Scoring        │   │ • Save           │
│ • Validación     │   │ • Load           │
│ • Asignación     │   │ • Export         │
└──────────────────┘   └──────────────────┘
          │                     │
          └──────────┬──────────┘
                     │
                     ▼
          ┌──────────────────┐
          │    CONSTANTS     │
          │   & TYPES        │
          │                  │
          │ • Médicos        │
          │ • Áreas          │
          │ • Restricciones  │
          └──────────────────┘
```

---

## Flujo de Datos: Generación de Cronograma

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Usuario hace clic en "Reasignar Turnos"                  │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. App.tsx llama generateSchedule(mes, año)                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. scheduler.ts procesa cada día laborable:                 │
│    ┌────────────────────────────────────────────┐           │
│    │ PARA cada día del mes:                     │           │
│    │   a. Obtener médicos disponibles ese día  │           │
│    │   b. Aplicar R1.4 (Exclusión C/A)         │           │
│    │   c. PARA cada área (Juncal, Pueyrredón, UC): │       │
│    │      - Calcular score de cada candidato   │           │
│    │      - Asignar médico con mayor score     │           │
│    │      - Remover de disponibles             │           │
│    └────────────────────────────────────────────┘           │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. validateSchedule() verifica reglas:                      │
│    • R1.3: ¿Todos los días tienen 3 profesionales?          │
│    • R1.4: ¿Cristina y Agustina nunca juntas?               │
│    • Advertencias: ¿Agustina en UC?                         │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. storageService.saveSchedule() guarda en LocalStorage     │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. App.tsx actualiza UI:                                    │
│    • Renderiza CalendarGrid con tarjetas de día             │
│    • Muestra indicador de validación (✅/❌)                 │
│    • Habilita botón "Exportar"                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Sistema de Scoring (Algoritmo CSP)

```
Para cada médico candidato en un área específica:

SCORE = SCORE_BASE
      + BONUS_PRIORIDAD_ÁREA
      + BONUS_ESPECIALIDAD
      - PENALIZACIÓN_EVITAR

┌──────────────────────────────────────────────────────────┐
│ EJEMPLO 1: Cristina evaluada para Piso Pueyrredón       │
├──────────────────────────────────────────────────────────┤
│ SCORE_BASE:                   0                          │
│ + R3.1 (Cristina→Pueyrredón): +100                       │
│ + R3.4 (Especialidad):         +0   (no coincide)        │
│ - R3.3 (Evitar):               -0   (no aplica)          │
│ ─────────────────────────────────                        │
│ SCORE TOTAL:                   100  ← ¡MÁXIMA PRIORIDAD! │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│ EJEMPLO 2: Agustina evaluada para Unidades Cerradas     │
├──────────────────────────────────────────────────────────┤
│ SCORE_BASE:                   0                          │
│ + R3.1:                       +0   (no aplica)           │
│ + R3.4:                       +0   (no coincide)         │
│ - R3.3 (Agustina⊗UC):         -50  ← PENALIZACIÓN       │
│ ─────────────────────────────────                        │
│ SCORE TOTAL:                  -50  ← EVITAR si posible   │
└──────────────────────────────────────────────────────────┘
```

---

## Resolución de Conflictos (R1.4)

```
PROBLEMA: Cristina y Agustina ambas disponibles el LUNES

┌────────────────────────────────────────────────────────┐
│ Solución: Sistema de Alternancia con contador toggle  │
└────────────────────────────────────────────────────────┘

DÍA 1 (Lunes 1):  toggle = 0 (par)
  ├─ Cristina: ✅ SELECCIONADA
  └─ Agustina: ❌ EXCLUIDA

DÍA 2 (Martes 2): toggle = 1 (impar)
  ├─ Cristina: ❌ EXCLUIDA
  └─ Agustina: ✅ SELECCIONADA

DÍA 3 (Lunes 8):  toggle = 2 (par)
  ├─ Cristina: ✅ SELECCIONADA
  └─ Agustina: ❌ EXCLUIDA

[... y así sucesivamente, garantizando distribución 50/50]
```

---

## Estructura de Componentes React

```
App.tsx (Componente Principal)
│
├── Sidebar.tsx
│   ├── Logo & Título
│   ├── Lista de Médicos
│   │   └── Card por médico (nombre, especialidad, días)
│   ├── Áreas de Cobertura
│   │   └── Badge por área (orden de prioridad)
│   └── Restricciones Activas
│       └── Card por restricción (código, nombre, peso)
│
├── CalendarGrid.tsx
│   ├── Header del Mes (nombre + estadísticas)
│   ├── Grid de Días
│   │   └── DayCard.tsx (por cada día laborable)
│   │       ├── Fecha + Día de semana
│   │       ├── 3 Áreas con asignaciones
│   │       └── Indicador de estado (✅/⚠️)
│   └── Resumen del Mes
│       └── 4 StatCards (Total, Completos, Incompletos, Asignaciones)
│
└── AssignmentModal.tsx (condicional)
    ├── Header con fecha
    ├── Advertencias (si hay)
    ├── 3 Dropdowns (uno por área)
    ├── Info de disponibilidad
    └── Botones (Cancelar / Guardar)
```

---

## Tipos de Datos (TypeScript)

```typescript
Doctor {
  id: string
  name: string
  availableDays: DayOfWeek[]  // [1,2,3] = Lun,Mar,Mié
  specialty: string
}

Assignment {
  doctorId: string
  doctorName: string
  area: AreaType              // 'Sector Juncal' | ...
}

DaySchedule {
  date: string                // ISO format
  dayOfWeek: DayOfWeek        // 0-6
  assignments: Assignment[]   // Array de 0-3
  isComplete: boolean         // true si length === 3
}

MonthSchedule {
  month: number               // 0-11
  year: number
  days: DaySchedule[]
  generatedAt: string         // ISO timestamp
}
```

---

## Matriz de Disponibilidad

```
           Lun  Mar  Mié  Jue  Vie
         ┌────┬────┬────┬────┬────┐
Leticia  │ ❌ │ ✅ │ ❌ │ ✅ │ ✅ │
         ├────┼────┼────┼────┼────┤
Agustina │ ✅ │ ✅ │ ✅ │ ❌ │ ❌ │ → Bloquea a Cristina
         ├────┼────┼────┼────┼────┤
Natalia  │ ❌ │ ✅ │ ✅ │ ❌ │ ✅ │
         ├────┼────┼────┼────┼────┤
Cristina │ ✅ │ ✅ │ ❌ │ ✅ │ ❌ │ → Bloquea a Agustina
         ├────┼────┼────┼────┼────┤
Cecilia  │ ❌ │ ❌ │ ✅ │ ✅ │ ✅ │
         └────┴────┴────┴────┴────┘

CONFLICTO: Lunes y Martes (Cristina ⊕ Agustina)
SOLUCIÓN: Alternancia automática en scheduler.ts
```

---

## Tabla de Prioridades por Área

```
┌────────────────────┬────────────┬─────────────────────┐
│ ÁREA               │ PRIORIDAD  │ MÉDICOS PREFERIDOS  │
├────────────────────┼────────────┼─────────────────────┤
│ Sector Juncal      │ ★★★ (1°)   │ Natalia, Leticia    │
│                    │            │ (+100 puntos)       │
├────────────────────┼────────────┼─────────────────────┤
│ Piso Pueyrredón    │ ★★☆ (2°)   │ Cristina            │
│                    │            │ (+100 puntos)       │
├────────────────────┼────────────┼─────────────────────┤
│ Unidades Cerradas  │ ★☆☆ (3°)   │ Cualquiera          │
│                    │            │ (Agustina: -50)     │
└────────────────────┴────────────┴─────────────────────┘

ORDEN DE LLENADO:
1º → Juncal (busca Natalia/Leticia)
2º → Pueyrredón (busca Cristina)
3º → Cerradas (asigna quien quede)
```

---

## Colores y Estados Visuales

```
┌─────────────────────────────────────────────────────┐
│ DÍA COMPLETO (3 profesionales)                      │
├─────────────────────────────────────────────────────┤
│ • Borde: Verde esmeralda (border-emerald-200)       │
│ • Fondo: Verde claro (bg-emerald-50)                │
│ • Icono: ✅ CheckCircle2 (text-emerald-600)         │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ DÍA INCOMPLETO (<3 profesionales)                   │
├─────────────────────────────────────────────────────┤
│ • Borde: Rojo rosa (border-rose-200)                │
│ • Fondo: Rojo claro (bg-rose-50)                    │
│ • Icono: ⚠️ AlertTriangle (text-rose-600)           │
│ • Badge: "⚠️ Cobertura incompleta (X/3)"            │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ RESTRICCIÓN HARD (Obligatoria)                      │
├─────────────────────────────────────────────────────┤
│ • Fondo: Rojo claro (bg-rose-50)                    │
│ • Borde: Rojo oscuro (border-rose-200)              │
│ • Texto: Rojo (text-rose-600)                       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ RESTRICCIÓN SOFT (Preferencia)                      │
├─────────────────────────────────────────────────────┤
│ • Fondo: Azul claro (bg-blue-50)                    │
│ • Borde: Azul medio (border-blue-200)               │
│ • Texto: Azul oscuro (text-blue-600)                │
└─────────────────────────────────────────────────────┘
```

---

## Performance y Optimización

```
┌────────────────────────────┬──────────┬─────────────┐
│ OPERACIÓN                  │ TIEMPO   │ COMPLEJIDAD │
├────────────────────────────┼──────────┼─────────────┤
│ Cargar aplicación          │ <100ms   │ O(1)        │
│ Generar cronograma (1 mes) │ <50ms*   │ O(n×m×k)    │
│ Validar cronograma         │ <10ms    │ O(n)        │
│ Guardar LocalStorage       │ <5ms     │ O(1)        │
│ Renderizar UI              │ 16ms     │ 60 FPS      │
└────────────────────────────┴──────────┴─────────────┘

* Incluye delay UX de 1s para feedback visual

DONDE:
n = días laborables (~23)
m = médicos disponibles (~5)
k = áreas (3)
```

---

## Tecnologías y Versiones

```
┌──────────────────┬──────────┬────────────────────────┐
│ TECNOLOGÍA       │ VERSIÓN  │ PROPÓSITO              │
├──────────────────┼──────────┼────────────────────────┤
│ React            │ 18.2.0   │ UI Framework           │
│ TypeScript       │ 5.2.2    │ Type Safety            │
│ Vite             │ 5.0.8    │ Build Tool             │
│ Tailwind CSS     │ 3.4.0    │ Styling                │
│ date-fns         │ 3.0.0    │ Date Management        │
│ Lucide React     │ 0.303.0  │ Icons                  │
└──────────────────┴──────────┴────────────────────────┘
```

---

Este documento proporciona una visión completa de la arquitectura del sistema InfectoPlan, facilitando el mantenimiento y futuras extensiones.

